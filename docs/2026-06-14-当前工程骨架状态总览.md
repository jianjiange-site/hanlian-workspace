# 2026-06-14 当前工程骨架状态总览

## 1. 文档目的

这份文档用来记录当前 `hanlian-workspace` 的工程骨架状态。

今天的目标不是写真实业务，而是把后续可以持续开发的基础链路搭出来：

- 服务能启动。
- 基础设施能连通。
- proto 能生成 Java stub。
- 服务之间能通过 gRPC 调用。
- 每一步都有验证方式和复盘文档。

这份文档相当于当前工程的“地图”。

---

## 2. 当前仓库结构

本地路径：

```text
E:\heart-dev\workspace\hanlian-workspace
```

远程仓库：

```text
https://github.com/jianjiange-site/hanlian-workspace.git
```

核心目录：

```text
hanlian-workspace/
  ai-chat/
  dating-server/
  proto/
  README.md
  .gitignore
```

目录说明：

- `ai-chat`：Python AI 服务骨架。
- `dating-server`：Java 后端多模块工程。
- `proto`：服务间 gRPC 契约。

---

## 3. 当前已完成能力

### 3.1 workspace 骨架

已经完成：

- 创建 `hanlian-workspace` 仓库。
- 创建 `ai-chat`、`dating-server`、`proto` 三个核心目录。
- `.gitignore` 已覆盖 Java、Python、IDE、本地环境变量和构建产物。

当前状态：

```text
workspace 骨架已成立。
```

---

### 3.2 dating-server Java 多模块工程

当前模块：

```text
dating-server/
  common/
  mobile-gateway/
  user-service/
  im-service/
  match-service/
  post-service/
  payment-service/
```

当前能力：

- 每个服务都有 Spring Boot 启动类。
- 每个服务都有 `/internal/ping`。
- 每个服务都有 `/actuator/health`。
- 父工程可以统一构建子模块。

当前状态：

```text
Java 服务骨架已成立。
```

---

### 3.3 Nacos Discovery

当前已接入：

- `mobile-gateway`
- `user-service`
- `im-service`
- `match-service`
- `post-service`
- `payment-service`

当前 namespace：

```text
dev-hanlian
```

配置方式：

```yaml
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}
```

当前状态：

```text
服务注册发现骨架已接入。
```

注意：

```text
当前 gRPC 调用链还没有通过 Nacos 发现 gRPC 地址。
mobile-gateway -> user-service 暂时使用 127.0.0.1:19081。
```

---

### 3.4 PostgreSQL / Redis 连通性

当前在 `user-service` 已完成最小连通性验证。

PostgreSQL：

```text
GET http://localhost:18081/internal/check/db
```

成功返回：

```json
{"result":"1","database":"ok"}
```

Redis：

```text
GET http://localhost:18081/internal/check/redis
```

成功返回：

```json
{"redis":"ok","ttlSeconds":"60","key":"hanlian:user-service:check:redis","value":"ok"}
```

使用的环境变量：

```text
POSTGRES_PASSWORD
REDIS_PASSWORD
NACOS_PASSWORD
```

当前状态：

```text
user-service 已经能连接共享 PostgreSQL 和共享 Redis。
```

---

### 3.5 proto/user Java 生成

当前已完成：

```text
proto/user -> Java stub -> 本地 Maven install
```

相关目录：

```text
proto/user
```

生成命令：

```powershell
cd E:\heart-dev\workspace\hanlian-workspace\proto\user
mvn clean install
```

本地安装位置：

```text
C:\Users\admin\.m2\repository\com\dating\hanlian\proto\user-proto\0.1.0
```

生成核心类：

```text
PingRequest
PingResponse
UserServiceGrpc
```

当前状态：

```text
user-proto Java 生成和本地安装已验证。
```

注意：

```text
当前没有 Nexus 凭据，所以只做本地 install，不做 deploy。
```

---

### 3.6 user-service gRPC server

当前 `user-service` 同时提供：

```text
HTTP: 18081
gRPC: 19081
```

启动成功日志：

```text
user-service gRPC server started on port 19081
```

grpcurl 验证命令：

```powershell
'{"message":"hello"}' | & C:\tmp\grpcurl\grpcurl.exe -plaintext -proto proto/user/src/main/proto/user.proto -d '@' localhost:19081 dating.user.v1.UserService/Ping
```

成功返回：

```json
{
  "message": "user-service pong: hello"
}
```

当前状态：

```text
user-service 已经能运行最小 gRPC 服务端。
```

---

### 3.7 mobile-gateway -> user-service gRPC 调用链

当前调用链：

```text
浏览器 / curl
  -> mobile-gateway HTTP 8080
  -> mobile-gateway gRPC client
  -> user-service gRPC 19081
  -> UserService/Ping
  -> 返回 pong
```

验证地址：

```text
http://localhost:8080/internal/check/user-grpc
```

成功返回：

```json
{"userGrpc":"ok","gateway":"ok","message":"user-service pong: hello from mobile-gateway"}
```

当前状态：

```text
第一条服务间 gRPC 调用链已打通。
```

---

## 4. 当前服务状态

| 服务 | HTTP 端口 | gRPC 端口 | 当前状态 |
|---|---:|---:|---|
| mobile-gateway | 8080 | 暂无服务端 | 已能通过 gRPC 调 user-service |
| user-service | 18081 | 19081 | 已接 PG / Redis / gRPC |
| im-service | 18082 | 暂无 | ping / health 骨架 |
| match-service | 18083 | 暂无 | ping / health 骨架 |
| post-service | 18084 | 暂无 | ping / health 骨架 |
| payment-service | 18085 | 暂无 | ping / health 骨架 |

说明：

```text
当前只有 user-service 实现了 gRPC server。
当前只有 mobile-gateway 实现了 gRPC client。
其他服务先保持骨架状态。
```

---

## 5. 当前验证命令

### 5.1 查看 Git 状态

```powershell
cd E:\heart-dev\workspace\hanlian-workspace
git status
```

### 5.2 构建 user-service

```powershell
cd E:\heart-dev\workspace\hanlian-workspace\dating-server
mvn -pl user-service -am -DskipTests package
```

### 5.3 构建 mobile-gateway

```powershell
cd E:\heart-dev\workspace\hanlian-workspace\dating-server
mvn -pl mobile-gateway -am -DskipTests package
```

### 5.4 启动 user-service

```powershell
cd E:\heart-dev\workspace\hanlian-workspace\dating-server\user-service
mvn spring-boot:run
```

### 5.5 启动 mobile-gateway

```powershell
cd E:\heart-dev\workspace\hanlian-workspace\dating-server\mobile-gateway
mvn spring-boot:run
```

### 5.6 验证 user-service

```powershell
curl http://localhost:18081/internal/ping
curl http://localhost:18081/internal/check/db
curl http://localhost:18081/internal/check/redis
```

### 5.7 验证 mobile-gateway

```powershell
curl http://localhost:8080/internal/ping
curl http://localhost:8080/internal/check/user-grpc
```

### 5.8 验证 gRPC 直连

```powershell
'{"message":"hello"}' | & C:\tmp\grpcurl\grpcurl.exe -plaintext -proto proto/user/src/main/proto/user.proto -d '@' localhost:19081 dating.user.v1.UserService/Ping
```

---

## 6. 当前复盘文档

已经沉淀的文档：

```text
2026-06-12-今日开发环境接入与工程骨架复盘.md
2026-06-13-Nacos-Discovery-接入与本地运行说明.md
2026-06-13-user-service-PG-Redis-接入复盘.md
2026-06-14-mobile-gateway-user-service-gRPC-调用链复盘.md
2026-06-14-当前工程骨架状态总览.md
```

本机学习资料目录还有：

```text
C:\Users\admin\Documents\心动\心动-git版本控制说明书.md
```

---

## 7. 当前还没有做

工程能力上还没有做：

- gRPC 通过 Nacos 服务发现。
- gRPC 负载均衡。
- gRPC 超时和重试配置。
- gRPC traceId 透传。
- gRPC 统一异常映射。
- common-starter 封装。
- Dockerfile / docker-compose.app.yml。
- 本地 Jenkins 构建部署。
- Loki / Grafana 本地日志链路。
- Nacos Config 配置中心。

业务能力上还没有做：

- 登录 / JWT。
- 真实用户表。
- user profile 业务。
- match-service feed / swipe。
- im-service 真实 OpenIM 编排。
- payment-service 订阅 / 金币。
- ai-chat 真实 LLM。

---

## 8. 为什么今天先不建业务表

今天目标是：

```text
搭出可以继续开发的骨架。
```

当前更重要的是：

- 服务边界清楚。
- proto 能生成。
- gRPC 链路能跑。
- 基础设施能连。
- 文档能复盘。

如果现在马上建业务表，会进入字段设计、索引设计、迁移脚本、实体层、Mapper 层、业务边界等问题，容易把“骨架阶段”变成“业务开发阶段”。

所以今天先收口工程骨架，后续再进入真实表结构。

---

## 9. 下一阶段建议

建议顺序：

1. 补充 `proto/README.md`，说明当前 Java 生成路线。
2. 总结一份 gRPC server/client 模板约定。
3. 把 `user-proto` 的模式复制到 `match-proto` 或 `im-proto`。
4. 决定下一条服务链路：
   - `mobile-gateway -> match-service`
   - 或 `match-service -> user-service`
5. 再决定是否进入 `user-service` 最小业务表。

推荐下一步：

```text
先补 proto/README.md，让 proto 目录成为后续所有服务生成 stub 的入口说明。
```

