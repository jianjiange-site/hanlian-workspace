server:
  port: 18082

spring:
  application:
    name: im-service
  cloud:
    nacos:
      discovery:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}

management:
  endpoints:
    web:
      exposure:
        include: health
