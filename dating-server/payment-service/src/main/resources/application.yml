server:
  port: 18085

spring:
  application:
    name: payment-service
  config:
    import: optional:nacos:payment-service.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}
      config:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: health

grpc:
  server:
    port: 19085
