server:
  port: 18084

spring:
  application:
    name: post-service
  cloud:
    nacos:
      discovery:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}

management:
  endpoints:
    web:
      exposure:
        include: health
