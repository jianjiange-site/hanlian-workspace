server:
  port: 18084

spring:
  application:
    name: post-service
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    table: flyway_history_post
  config:
    import: optional:nacos:post-service.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}
      config:
        server-addr: 38.76.188.242:8848
        namespace: dev-hanlian
        username: nacos
        password: ${NACOS_PASSWORD}
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: health

grpc:
  server:
    port: 19084
